Algoritmo "teste_mesa"

Var

   i, x, y : inteiro

Inicio

      x <- 8
      y <- 3
      
      para i de 0 ate 3 faca

           x <- x - 2
           Y <- y + 1
           
           escreval(x)
           escreval(y)
           escreval("******")
           
      fimpara
      escreva(i)

Fimalgoritmo