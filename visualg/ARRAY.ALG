Algoritmo "array"

Var

   i : inteiro
   v : vetor [0..3] de inteiro
   w : vetor [0..3] de inteiro

Inicio
      
      v[0] <- 2
      
      para i de 0 ate 3 faca
               w[i] <- v[i] * 10
      fimpara
      
       para i de 0 ate 3 faca
           escreval(w[i])
      fimpara

Fimalgoritmo